
链路聚合：全称以太网链路聚合技术（Link Aggregation）,是一种端口捆绑、链路捆绑技术，简单来说就是把两条或者多条物理链路，聚合成一条虚拟的逻辑链路Eth-Trunk，
主要解决2个问题：1、在不增加硬件投入，不增加成本的情况下增加带宽，解决带宽不足的问题；2、解决单点故障问题，一旦一条物理链路发生故障，其他链路仍能正常工作，从而保障网络的可靠性。
如果不做链路聚合，直接交换机之间多连几根线。就会造成环路，导致网络风暴。
此时如果开了STP生成树协议，就会阻塞掉其他端口，仍然只保留一条链路转发数据，起不到链路聚合的功能。
华为交换机上Eth-trunk支持两种工作方式：手工负载分担（Manual load-balance）和LACP
手工负载分担中所有接口均处于转发状态，分担负载的流量。Eth-trunk的创建、成员接口的加入都需要手工配置，没有LACP（Link Aggregation Control Protocol链路聚合控制协议）报文的参与。
基础配置命令：
#创建eth-trunk
int eth-trunk 1
#指定聚合接口工作方式为手工负载分担,链路聚合两端的设备需要使用相同的工作方式。
mode manual load-balance
#添加物理接口到eth-trunk 1中
int g0/0/1
eth-trunk 1
int g0/0/2
eth-trunk 1
int g0/0/3
eth-trunk 1
#配置eth-trunk成员链路的负载分担模式：load-balance { dst-ip | src-ip | src-dst-ip | dst-mac | src-mac | src-dst-mac | round-robin | enhanced [resilient] profile <profile-name> }
eth-trunk 1
load-balance ***
#为保证eth-trunk的状态和带宽，可以设置活动接口数量的阈值（缺省值为1），当活动链路小于下限阈值时，eth-trunk接口状态转为down
least active-linknumber *

LACP模式下，需要手工创建eth-trunk，手工加入eth-trunk成员接口，但是，由LACP协议协商确定活动接口和非活动接口。
LACP模式也称为M:N模式，即M条链路处于活动状态，N条链路处于非活动状态作为备份链路。当M条链路中有链路出现故障时，系统会从备份链路中选择优先级最高的接替出现故障的链路。
LACP模式与手工负载分担模式的主要区别是：LACP模式有备份链路。而手工负载分担模式没有备份链路，所有成员接口均处于转发状态，分担负载流量。
LACP模式中优先级值越小，优先级越高，为主动端。优先级低的一端为被动端，若优先级一样时，则MAC地址小的为主动端。
#创建eth-trunk，并将工作方式修改为LACP
int eth-trunk 1
mode lacp-static#缺省情况下，为手工负载分担方式。
#添加物理接口到eth-trunk 1中
int g0/0/1
eth-trunk 1
int g0/0/2
eth-trunk 1
int g0/0/3
eth-trunk 1
#配置eth-trunk成员链路的负载分担模式：load-balance { dst-ip | src-ip | src-dst-ip | dst-mac | src-mac | src-dst-mac | round-robin | enhanced [resilient] profile <profile-name> }
eth-trunk 1
load-balance ***
#配置设备LACP优先级,缺省情况下，系统LACP优先级32768.
lacp priority *
#配置接口LACP优先级,接口优先级（值越小）越高越有可能成为活动的成员接口
int g0/0/1
lacp priority *
#配置活动接口上限阈值,缺省状态下，活动接口上限阈值为8
eth-trunk 1
max active-linknumber *
#配置活动接口下限阈值，缺省状态下，活动接口下限阈值为1
eth-trunk 1
least active-linknumber *
#使能LACP抢占，配置抢占等待时间。缺省状态下，lacp抢占功能处于禁止状态，lacp抢占等待时间为30秒。
#使能LACP抢占功能可以保持接口LACP优先级最高的接口为活动接口。例如：当一条高优先级的接口因故障切换为非活动状态而后又恢复时，如果使能了抢占，则高优先级接口将重新成为活动接口;如果未使能抢占，该接口不能重新成为活动接口。
eth-trunk 1
lacp preempt enable
lacp preempt delay **(time)

#由于eth-trunk1聚合接口需要承载多个VLAN的二层流量，因此需配置为trunk类型。注意一旦聚合接口配置完成后，针对该逻辑接口的相关配置就在inteface eth-trunk中进行，也就是在产生的聚合接口中完成,而不是在GE0/0/23或GE0/0/24口的接口中进行。
#查看配置
dis eth-trunk 1


注意：Eth-Trunk接口中只能添加GE接口、XGE接口,删除Eth-Trunk时，Eth-Trunk中不能有成员接口

同一个Eth-Trunk的所有成员接口的以上属性必须保持一致，不能单独修改。修改Eth-Trunk的以上属性，它的所有成员接口的对应属性也相应改变。

在接口加入Eth-Trunk之前，建议首先使用shutdown（接口视图）命令关闭接口。在两端的接口都加入Eth-Trunk后，使用undo shutdown命令开启接口，否则可能会出现流量中断或短暂广播风暴的现象。
在接口从Eth-Trunk中删除之前，建议首先使用shutdown（接口视图）命令将一个成员接口关闭再使用undo eth-trunk命令将接口从Eth-Trunk中删除，按照此方式删除其它接口。

interface XGigabitEthernet0/0/3                                                 
 undo negotiation auto                                                          
 eth-trunk 34    
interface XGigabitEthernet0/0/4                                                 
 undo negotiation auto                                                          
 eth-trunk 34                                                                
                                                                          
interface Eth-Trunk34                                                           
 port link-type access                                                          
 port default vlan 34                                                           
 mode lacp 

静态LACP模式与手工负载分担模式的主要区别为：静态LACP模式有备份链路，而手工负载分担模式所有成员接口均处于转发状态，分担负载流量。

sys
int eth-trunk 10
trunkport g0/0/11 g0/0/13
trunkport g0/0/1 t g0/0/4

sys
int eth-trunk 10
q
int g0/0/1
eth-trunk 2

查看eth-trunk接口的配置信息：
dis eth-trunk
dis eth-trunk 10
dis int eth-trunk 10


Eth-Trunk：一般指同一设备的链路聚合，一台交换机将多个接口捆绑，形成一个Eth-Trunk接口。
Eth-Trunk优点
负载分担：通过Trunk接口可以实现负载分担。在一个Eth-Trunk接口内，可以实现流量负载分担。
提高可靠性：当某个成员接口连接的物理链路出现故障时，流量会切换到其他可用的链路上，从而提高整个Trunk链路的可靠性。
增加带宽：Trunk接口的总带宽是各成员接口带宽之和

E-Trunk（Enhanced Trunk）：一般指跨设备链路聚合，是一种实现跨设备链路聚合的机制，基于LACP（单台设备链路聚合的标准）进行了扩展，能够实现多台设备间的链路聚合。
从而把链路可靠性从单板级提高到了设备级。
E-Trunk机制主要应用于CE双归接入VPLS、VLL、PWE3网络时，CE与PE间的链路保护以及对PE设备节点故障的保护。
在没有使用E-Trunk前，CE通过Eth-Trunk链路只能单归到一个PE设备。如果Eth-Trunk出现故障或者PE设备故障，CE将无法与PE设备继续进行通信。
使用E-Trunk后，CE可以双归到PE上,从而实现设备间保护。

如果两台设备做的CSS集群，那么两台设备可以看作是一台设备，这两台物理设备之间的链路聚合是通过Eth-trunk实现的，相当于同一台设备的链路聚合，而非跨设备的链路聚合。

只有静态LACP模式的Eth-Trunk可以加入E-Trunk。一个Eth-Trunk只能加入一个E-Trunk。
如果该Eth-Trunk已经加入其他E-Trunk时，必须退出原有E-Trunk，才能加入新的E-Trunk。
一个E-Trunk最多可以加入64个静态LACP模式的Eth-Trunk。

举例：将eth-trunk 10 加入ID为10的e-trunk
sys
int eth-trunk 10
e-trunk 10


只能对已经加入E-Trunk的Eth-Trunk执行e-trunk mode XXX。当Eth-Trunk退出E-Trunk时，该配置将自动清除。
sys
int eth-trunk 10
e-trunk mode auto|force-master|force-backup



