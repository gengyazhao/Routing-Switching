端口镜像常用于：流量观测及统计；故障定位等。
端口镜像就是将某个特定端口的流量拷贝到某个监控端口。

基于端口的镜像：端口镜像就是将被监控端口上的数据复制到指定的监控端口，对数据进行分析和监视。
基于流的镜像：流镜像就是将匹配访问控制列表的业务流复制到指定的监控端口，用于报文的分析和监视。

以太网交换机支持多对一的镜像，即将多个端口的报文复制到一个监控端口上。
端口镜像又分为本地端口镜像和远程端口镜像：本地端口镜像中，监控PC与观察端口直接相连；远程端口镜像中，监控PC与观察端口所在设备之间通过二层网络或三层网络相联。

二层端口镜像（RSPAN：Remote Switched Port Analyzer）:将镜像端口的报文封装VLAN，然后通过观察端口将报文在远程镜像VLAN中进行广播。远程的设备收到报文后，比较报文的vlan-id，如果相同，则将报文转发到远程观察端口。
三层端口镜像（ERSPAN：Encapsulated Remote SPAN）：若通过三层网络互联，以S9300交换机为例，它使用GRE报文头封装和解封装镜像报文，使得镜像报文可以穿透三层网络，从而实现镜像端口所在设备与观察端口所在设备之间通过三层网络相连时的端口镜像。

配置本地端口镜像：
#将交换机GE0/0/2端口的进、出口报文镜像到GE0/0/3
observe-port 1 int g0/0/3
int g0/0/2
port-mirroring to observe-port 1 both

常用的报文分析工具：Wireshark、Sniffer
观察端口专门用于故障定位或流量分析，不通用作普通业务口。

捕获业务报文
在实际项目中，在进行问题定位及分析时，我们可能并不具备在交换机上进行端口镜像的条件，例如管理PC不在机房，工程师通过远程登录的方式连接到网络设备，而如果此时又需要进行报文捕获，那么就可以使用capture-packet命令。
eg1:在交换机上抓取接口g0/0/1收到的报文，并将捕获的报文呈现在终端界面上。
[SW]capture-packet int g0/0/1 destination terminal
#交换机会持续在终端界面上显示捕获到的报文，直到捕获超时（缺省60S）

eg2:捕获交换机接口上收到的特定报文，并将捕获结果输出到终端界面。telnet协议号23
#使用acl 3000来匹配需要的报文流量，然后用capture-packet执行报文捕获
acl 3000
rule permit tcp source 1.1.1.1 0 destination 1.1.1.2 0 destination-port eq 23
quit
capture-packet int g0/0/1 acl 3000 destination terminal
#另外，在capture-packet int g0/0/1 acl 3000 destination terminal命令的基础上增加file关键字并指定文件名，如：capture-packet int g0/0/1 acl 3000 destination terminal file test.cap
#那么当交换机捕获到报文后，不仅会在终端界面显示，还会保存到该网络设备的存储空间中，这样抓包完成后，可以将该文件下载到管理PC，再通过抓包软件进行分析。
再者,在capture-packet命令中使用timeout关键字,还可定义报文捕获的超时时间;使用vlan关键字，可以指定捕获的vlan。该命令目前只支持捕获接口入方向报文，不支持捕获接口出方向报文。
捕获的报文有速率限制，缺省值是64kbps，如果有突发流量，超过捕获报文的速率限制，可能会存在丢包现象。最后，设备不支持捕获BFD和802.1ag。
